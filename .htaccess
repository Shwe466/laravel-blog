<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /cain_renshuyou/

    # Redirect all requests to the public folder without showing it in the URL
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>